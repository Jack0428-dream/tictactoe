function Gameboard() {
    const grid = 3;
    const gameboard =[];

    for(let i = 0; i < grid; i++) {
        gameboard[i] = [];
        for (let j = 0; j < grid; j++) {
            gameboard[i].push(null);
        }
    }

    const getboard = () => gameboard;

    // const indexChoice = (index, ) => {
    //     if ( index == 1 ) {

    //     }
    // }
}

function indexChoice(index) {
    
}